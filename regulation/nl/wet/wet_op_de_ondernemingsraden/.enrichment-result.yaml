# .enrichment-result.yaml — result envelope, NOT part of the law schema
law_id: wet_op_de_ondernemingsraden
related_legislation:
  # No external law dependencies in the machine_readable computations.
  # The WOR references several laws in legal text (Pensioenwet, Wet financiering
  # sociale verzekeringen, Wet bescherming klokkenluiders) but none of these are
  # needed as source.regulation bindings for the computable articles.
  []
